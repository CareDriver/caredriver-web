import { Metadata } from "next";

export const metadata: Metadata = {
    title: "No encontrado",
    description: "",
};

const Page = () => {
    return <h1>custom not found</h1>;
};

export default Page;
